# Supabase connection for the Michelscom valuation dashboard.
# These are PUBLISHABLE (client-side) credentials — safe to expose in a browser
# because the database is protected by row-level security (read-only public access).
# Copy this file to `.env` to override the built-in defaults during local dev.

VITE_SUPABASE_URL=https://mniktvbvgnjedidfjrfj.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_neMFaXR5OcARTPTfr5e4KA_sRqAz6NK
